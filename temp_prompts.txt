
### 6. Fix API-key dashboard onboarding without weakening security

```text
Inspect api/deps.py, api/main.py, dashboard/index.html, dashboard/dashboard.js, config/settings.py, .env.example, README.md, and API tests. Preserve API-key protection for non-public endpoints when API_KEY is configured, and preserve public dashboard static assets.

Improve the dashboard user experience: distinguish 401/403 authentication failures from network/server failures; display a clear non-secret prompt explaining that the user must enter the locally configured API key; retry cleanly after saving the key; and allow a documented development mode with API auth intentionally disabled. Never render, log, return, or embed API_KEY/SECRET_KEY values. Confirm config public-dump redaction remains correct.

Add browser-independent/API tests for public paths, protected JSON paths with/without a key, and safe configuration serialization. Update setup documentation. Run ruff and tests, making no unrelated UI redesign.
```

### 7. Make Craigslist collector options real or remove them

```text
Inspect config/settings.py, core/scheduler.py, collectors/craigslist.py, core/networking.py, README.md, .env.example, and collector tests. Reconcile the advertised CollectorConfig fields with actual behavior.

At minimum, propagate `request_timeout` to the network request instead of the current hard-coded timeout. Decide whether pagination_limit and rate_limit_per_minute are supported for Craigslist. If supported, implement bounded pagination and per-collector request pacing with deterministic fixture-based tests; if not, remove or clearly mark the unused fields so users are not misled. Do not send credentials to Craigslist or log any credential values. Keep robots/rate-limit behavior conservative and avoid live network tests.

Add tests proving each supported setting has an observable effect, update docs/examples, run ruff and tests, and summarize the final supported configuration contract.
```

### 8. Modernize the Pydantic deprecation

```text
Inspect core/events/base.py and all event model tests. Replace Pydantic v2 deprecated class-based Config usage with ConfigDict/model_config while preserving event serialization, validation, and public field behavior. Add or update a focused regression test. Run ruff and the event/collector tests. Do not make unrelated event-bus changes.
```

## Master repair prompt

```text
Work on the MAIE marketplace repository as a focused repair release. Begin by reading AGENTS.md, pyproject.toml, README.md, config/settings.py, app/main.py, api/main.py, core/scheduler.py, collectors/base.py, core/pipeline_stages.py, analytics/warehouse.py, analytics/queries.py, api/deps.py, dashboard/dashboard.js, and existing tests. Check git status first and preserve unrelated user changes.

Repair the product's broken end-to-end path in this order:
1) Make clean setup reproducible from uv.lock and keep local secrets, venvs, caches, logs, and databases out of deliverables.
2) Provide and document a supported FastAPI/dashboard serve command; retain existing Typer CLI compatibility.
3) Make dashboard/API authentication understandable without exposing secrets.
4) Wire collector output through the intended valuation, score, opportunity, review queue, and notification workflow with idempotency and fixture-backed integration coverage.
5) Implement an explicit, configuration-controlled scheduler lifecycle with safe FastAPI startup/shutdown semantics.
6) Provide a supported analytics sync command (and protected API endpoint if appropriate), with a graceful no-snapshot response and dashboard state.
7) Make Craigslist config fields truthful: implement or remove unsupported timeout/pagination/rate-limit behavior.
8) Address the Pydantic Config deprecation.

Constraints: do not use live marketplace calls in tests; do not add external infrastructure such as Redis/Celery; do not weaken authentication; do not expose or modify real credentials; do not discard existing work; do not perform broad refactors. Use typed configuration with safe defaults, temporary paths in tests, and clear errors for user-actionable missing state.

After implementation, run `uv sync --locked --all-groups`, `uv run ruff check` on source, `uv run pytest`, and `uv build`. If an environment restriction prevents a command, state the exact restriction and still run all feasible targeted checks. Finish with a concise change summary, commands/results, remaining limitations, and any migration steps for an existing local database.
```
